# -*- coding: utf-8 -*-
#!/usr/bin/python

class Regex:
  def __init__(self, nombre, caso_base, valor, unario, cant_argumentos, argumentos ):
    self.nombre = nombre
    self.caso_base = caso_base
    self.unario = unario
    self.cant_argumentos = cant_argumentos
    self.argumentos = argumentos
